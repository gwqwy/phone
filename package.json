{
  "name": "zcode-phone",
  "version": "0.1.0",
  "private": true,
  "description": "手机远控编码智能体：ZCode / DeepSeek Harness 等 harness 的统一移动端控制台",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "setup": "npm --prefix server install",
    "start": "npm --prefix server run start",
    "dev:server": "npm --prefix server run dev",
    "test": "npm --prefix server run test"
  },
  "uniappClient": {
    "note": "client/ 是 HBuilderX 项目：用 HBuilderX 打开 client 目录，运行/发行 H5 后产物在 client/unpackage/dist/build/h5，由 server 直接托管"
  }
}
