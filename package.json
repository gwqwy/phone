{
  "name": "shou",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "shou —— 手机远控编码智能体：ZCode 与 DeepSeek Harness 的统一控制台",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "probe": "node --use-system-ca tools/probes/doctor.mjs"
  },
  "uniapp": {
    "note": "client/ 是 HBuilderX 项目，HBuilderX 直接打开 client 目录即可运行/发行。根目录的 package.json 只用于跑单测与探针，不参与打包。"
  },
  "devDependencies": {
    "vue": "^3.5.43"
  }
}
