{
  "name": "zphone-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "dev": "node --watch --enable-source-maps src/index.ts",
    "start": "node --enable-source-maps src/index.ts",
    "test": "node --test test/"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0"
  }
}
